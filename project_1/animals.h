/*
int update_hare(int current_position)
determines the new position of the hare after 1 second

Parameters:
    current_position: the current position of the hare
Returns:
    the new position of the hare after one time step
*/
extern int update_hare(int current_position);

/*
int update_tortoise(int current_position)
determines the new position of the tortoise after 1 second

Parameters:
    the current position of the tortoise
Returns:
    the new position of the tortoise after one time step
*/
extern int update_tortoise(int current_position);
